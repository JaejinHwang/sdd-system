# /validate [target]

스펙 파일의 유효성을 검사합니다.

## 개요

검사 항목:
- YAML 문법 오류
- 필수 필드 누락
- ID 참조 무결성 (US-001 → F-001 등)
- 규칙 준수 (Given-When-Then 형식 등)
- 일관성 (중복 ID, 순환 의존성 등)

## 사용법
````
/validate              # 전체 스펙 검증
/validate discovery    # Discovery 단계만
/validate spec         # Specification 단계만
/validate [파일명]     # 특정 파일만
````

## 프로세스

### 1. 검증 시작
````
🔍 스펙 검증 시작

검증 대상: 전체 스펙 (9개 파일)
검증 규칙: 23개

검증 중...
````

### 2. 검증 항목별 결과

#### 2.1 문법 검증
````
📝 YAML 문법 검증
✅ idea-crystal.yaml - 유효
✅ problem-definition.yaml - 유효
✅ requirements.yaml - 유효
✅ functional-spec.yaml - 유효
❌ technical-spec.yaml - 오류
   └─ Line 45: 잘못된 들여쓰기
✅ ui-spec.yaml - 유효
````

#### 2.2 필수 필드 검증
````
📋 필수 필드 검증

requirements.yaml:
✅ user_stories[].id - 모두 존재
✅ user_stories[].as_a - 모두 존재
✅ user_stories[].i_want - 모두 존재
✅ user_stories[].acceptance_criteria - 모두 존재
⚠️ user_stories[2].so_that - 누락

functional-spec.yaml:
✅ features[].id - 모두 존재
✅ features[].states - 모두 존재
❌ features[3].error_cases - 누락 (필수!)
⚠️ features[5].edge_cases - 1개만 있음 (권장: 2개 이상)
````

#### 2.3 참조 무결성 검증
````
🔗 참조 무결성 검증

requirements → functional:
✅ US-001 → F-001, F-002
✅ US-002 → F-003
❌ US-003 → F-007 (F-007 존재하지 않음!)

functional → technical:
✅ F-001 → /api/documents
⚠️ F-004 → /api/bookmarks (endpoint 미정의)

traceability.yaml:
✅ requirements_to_features: 유효
❌ features_to_code: F-003 매핑 누락
````

#### 2.4 규칙 준수 검증
````
📏 규칙 준수 검증

Given-When-Then 형식:
✅ US-001.acceptance_criteria[0] - 유효
✅ US-001.acceptance_criteria[1] - 유효
❌ US-002.acceptance_criteria[0] - 형식 오류
   └─ "PDF 업로드하면 파싱된다" 
   └─ → "Given [조건], When [행동], Then [결과]" 형식 필요

Error Cases 최소 개수 (3개):
✅ F-001: 4개
✅ F-002: 3개
❌ F-003: 2개 (1개 부족)

상태 전이 완전성:
✅ F-001: 모든 상태에서 전이 정의됨
⚠️ F-002: 'error' 상태에서 나가는 전이 없음
````

#### 2.5 일관성 검증
````
🔄 일관성 검증

중복 ID:
✅ User Story IDs - 중복 없음
❌ Feature IDs - F-003 중복 발견!
   └─ functional-spec.yaml:45
   └─ functional-spec.yaml:120

순환 의존성:
✅ Feature dependencies - 순환 없음
⚠️ Task dependencies - TASK-005 → TASK-007 → TASK-005 순환!

버전 일관성:
✅ changelog 최신 버전과 스펙 일치
````

### 3. 검증 결과 요약
````
📊 검증 결과 요약
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

전체 결과: ❌ 실패 (수정 필요)

✅ 통과: 18개 규칙
❌ 실패: 5개 규칙
⚠️ 경고: 4개 규칙

심각도별:
🔴 Critical (즉시 수정): 2개
🟠 Error (수정 권장): 3개  
🟡 Warning (검토 권장): 4개

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔴 Critical Issues:
1. technical-spec.yaml:45 - YAML 문법 오류
2. functional-spec.yaml - F-003 ID 중복

🟠 Errors:
1. US-003 → F-007 참조 오류
2. F-003 error_cases 누락
3. US-002 acceptance_criteria 형식 오류

🟡 Warnings:
1. US-002 so_that 필드 누락
2. F-005 edge_cases 1개만 존재
3. F-002 error 상태 전이 없음
4. Task 순환 의존성

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
````

### 4. 자동 수정 제안
````
🔧 자동 수정 가능한 항목

1. [YAML 문법] technical-spec.yaml:45
   들여쓰기 자동 수정 가능

2. [ID 중복] F-003 → F-006으로 변경
   연관 참조도 함께 수정

3. [형식 오류] US-002.acceptance_criteria[0]
   "PDF 업로드하면 파싱된다"
   → "Given PDF 파일이 있을 때, When 업로드하면, Then 파싱된다"

자동 수정을 진행할까요? (y/n/선택:1,2)
````

## 완료 메시지
````
✅ 검증 완료!

결과: 모든 규칙 통과
검증된 파일: 9개
검증된 규칙: 23개

스펙이 유효합니다. 구현을 진행해도 좋습니다.
````

또는
````
❌ 검증 실패

Critical 이슈 2개를 먼저 수정해야 합니다.
/validate 다시 실행하여 확인하세요.
````